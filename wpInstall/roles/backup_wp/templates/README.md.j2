# APPLICATION REQURES CONFIG.BASH IN THE APPLICATION DIRECTORY IN ORDER TO FUNCTION PROPERLY

# procedure of restoring / backupping:

# backup
1) check if config files are alight (paths to save the backup and what to backup)
2) run script newWeek.bash (once per week)
3) run script backup.bash (once per day)

# restore
1) run script restore.bash and answer the prompts


